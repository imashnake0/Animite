query MediaQuery($id: Int, $type: MediaType) {
    media: Media (id: $id, type: $type) {
        id
        type
        title {
            romaji
            english
            native
        }
        coverImage {
            extraLarge
            large
        }
    }
}
