query AnimeQuery($id: Int) {
    # TODO: Make this `MediaQuery`.
    media: Media (id: $id, type: ANIME) {
        id
        type
        title {
            romaji
            english
            native
        }
        coverImage {
            extraLarge
            large
        }
    }
}
