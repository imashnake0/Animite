query TrendingNow($type: MediaType) {
    # TODO: Pagination.
    media: Media(type: $type, sort: [TRENDING_DESC]) {
        id
        type
        title {
            romaji
            english
            native
        }
        coverImage {
            extraLarge
            large
        }
    }
}
