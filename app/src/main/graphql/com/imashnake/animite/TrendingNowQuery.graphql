query TrendingNow($type: MediaType, $page: Int, $perPage: Int) {
    page: Page (page: $page, perPage: $perPage) {
        media(type: $type, sort: [TRENDING_DESC]) {
            id
            type
            title {
                romaji
                english
                native
            }
            coverImage {
                extraLarge
                large
            }
        }
    }
}
