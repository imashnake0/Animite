mutation SaveMediaListEntry(
    $status: MediaListStatus,
    $mediaId: Int!,
    $score: Float
) {
    SaveMediaListEntry(
        status: $status,
        mediaId: $mediaId,
        score: $score
    ) {
        mediaId
    }
}
