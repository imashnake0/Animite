mutation UpdateUser(
    $profileColor: String,
    $animeListOptions: MediaListOptionsInput
) {
    UpdateUser(
        profileColor: $profileColor,
        animeListOptions: $animeListOptions
    ) {
        options {
            profileColor
        }
        mediaListOptions {
            animeList {
                sectionOrder
            }
        }
    }
}
